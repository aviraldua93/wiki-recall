{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://wikirecall.dev/schemas/knowledge-entity.schema.json",
  "title": "WikiRecall Knowledge Entity",
  "description": "Schema for a Karpathy-style knowledge entity — a mental model stored as Markdown with YAML frontmatter.",
  "type": "object",
  "required": ["title", "type", "updated"],
  "properties": {
    "title": {
      "type": "string",
      "minLength": 1,
      "maxLength": 256,
      "description": "Human-readable name of the entity."
    },
    "type": {
      "type": "string",
      "enum": ["platform", "system", "repo", "tool", "concept", "person", "team"],
      "description": "Classification of what this entity represents."
    },
    "updated": {
      "type": "string",
      "format": "date",
      "description": "ISO 8601 date when the entity was last updated (YYYY-MM-DD)."
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "uniqueItems": true,
      "default": [],
      "description": "Searchable tags for categorization."
    },
    "related": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "uniqueItems": true,
      "default": [],
      "description": "IDs of related knowledge entities."
    },
    "created": {
      "type": "string",
      "format": "date",
      "description": "ISO 8601 date when the entity was created (YYYY-MM-DD)."
    },
    "sources": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "default": [],
      "description": "Source document paths or identifiers this entity was derived from."
    },
    "source_count": {
      "type": "integer",
      "minimum": 0,
      "description": "Number of distinct source documents backing this entity."
    },
    "status": {
      "type": "string",
      "enum": ["draft", "reviewed", "needs_update"],
      "description": "Entity lifecycle status per Karpathy methodology."
    }
  },
  "additionalProperties": false
}
